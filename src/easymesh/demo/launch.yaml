coordinator:
  authkey: demo
nodes:
  receiver:
    command:
    - python
    - -m
    - easymesh.demo.receiver
    post_delay: 1
  sender:
    command:
      - python
      - -m
      - easymesh.demo.sender
      - --interval=1